from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Recruitment dashboard based on Workable for internal usage only',
    author='Maarten',
    license='MIT',
)
